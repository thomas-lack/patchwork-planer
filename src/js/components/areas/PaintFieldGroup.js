import React from 'react';

const PaintFieldGroup = ({size, onColumnsChange, onRowsChange}) => {
	return (
		<div className="paintField">
			<h3>Draw your pattern</h3>
		</div>
	);
};

export default(PaintFieldGroup);